@model IEnumerable<Pamela_4._0.Models.Periodo>

@{
    ViewBag.Title = "Index";
}

<h2>Periodos</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.FechaInicio)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FechaFinal)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.FechaInicio)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FechaFinal)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IDPeriodos }) |
            @Html.ActionLink("Details", "Details", new { id=item.IDPeriodos }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IDPeriodos })
        </td>
    </tr>
}

</table>
